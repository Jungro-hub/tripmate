header.header
    .header__column
        a(href=routes.home)
          i.fas.fa-tenge 트립메이트
    .header__column
        ul
            if user.isAuthenticated
                li
                    a(href=`${routes.schedules}${routes.upload}`) 업로드
                li
                    a(href=routes.userDetail(user.id)) 내 정보
                li
                    a(href=routes.logout) 로그아웃
            else
                li
                    a(href=routes.join) 가입하기
                li
                    a(href=routes.login) 로그인